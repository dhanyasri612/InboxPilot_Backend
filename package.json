{
  "name": "inboxpilot",
  "private": true,
  "scripts": {
    "backend": "uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000",
    "frontend": "npm run dev --prefix frontend",
    "dev": "echo 'Terminal 1: npm run backend   Terminal 2: npm run frontend'"
  }
}
